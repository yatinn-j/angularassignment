export class Ingredient {
      // this short-hand allows to declare properties with same name and 
      // also assign values to these propoerties at the time of class initialization
      constructor(public name: string, public amount: number) { }
}